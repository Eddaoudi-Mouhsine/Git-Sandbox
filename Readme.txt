1-Git is a version control system.
2-to see which version of you git you currently have git --version
3-Pull the latest version of the project to a local copy
4-Push local updates to the main project
5-When a file is changed, added or deleted, it is considered modified
6-You select the modified files you want to Stage
7-git config --global user.name "w3schools-test"
8-git config --global user.email "test@w3schools.com"
9-//Use global to set the username and e-mail for every repository on your computer.
10-If you want to set the username/e-mail for just the current repo, you can remove global
11-Once you have navigated to the correct folder, you can initialize Git on that folder:git init 
12-(Git creates a hidden folder to keep track of changes)
13-When you first add files to an empty repository, they are all untracked. To get Git to track them,
 you need to stage them, or add them to the staging environment.
 14-